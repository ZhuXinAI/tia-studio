## Fixed

- Make the `pnpm-workspace.yaml` config compatible with the GitHub Actions `pnpm@9` runner so CI can install dependencies again.
- Bundle both Intel and Apple Silicon libsql native bindings for macOS universal releases and keep electron-builder from re-merging them.

## Notes

- This patch supersedes the failed `0.1.23` rollout and restores a green CI/release path for the sidebar thread update shipped in `0.1.22`.
