## Fixed

- Ship the standard macOS release as Apple Silicon (`arm64`) only instead of attempting a universal app bundle.
- Keep the separate macOS Intel build isolated in its own workflow so it never changes the normal Windows or arm64 mac release path.
- Preserve the native `libsql` packaging fixes for Windows and the isolated Intel build workflow.

## Notes

- `v0.1.27` published the Windows artifacts, but the macOS universal release failed during packaging. This tag replaces that macOS release attempt with an arm64-only flow.
