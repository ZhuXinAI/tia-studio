## Fixed

- Stop triggering a second `CI` run on `v*` tag pushes by keeping `CI` on branch pushes only and letting release workflows trigger directly from tags.
- Ship the standard macOS release as Apple Silicon (`arm64`) only through the repo's `run-electron-builder` wrapper.
- Keep the separate macOS Intel build isolated in its own workflow so it does not affect the normal Windows or arm64 mac release path.

## Notes

- `v0.1.28` was a staging tag while the workflow wiring was still being adjusted and did not publish a release.
