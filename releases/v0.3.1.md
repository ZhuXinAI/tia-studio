# v0.3.1

## Highlights

- Removed the assistant coding-agent setup from the assistant editor so assistants no longer expose Codex-specific configuration in the dialog.
- Removed the dedicated coding subagent wiring from assistant and team runtimes to avoid unstable Codex CLI handoff behavior for now.
- Cleaned up the unused Codex bridge, dependency wiring, and release metadata so the app ships without the current coding-agent path.

## Notes

- `v0.3.1` is a patch release focused on backing out the unfinished coding-agent flow while the a2s specification is still being finalized.
