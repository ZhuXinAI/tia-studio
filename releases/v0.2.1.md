# v0.2.1

## Highlights

- Fixed the TIA browser tool runtime test harness so browser-window visibility and debugger startup checks run cleanly in CI.
- Fixed the thread page controller ref update to satisfy the React refs rule without changing runtime behavior.
- Stabilized the claw creation and channel-management test flows so the full renderer suite no longer flakes under CI load.

## Notes

- `v0.2.1` is a patch release focused on pipeline stability and release readiness after `v0.2.0`.
