## Fixed

- Fix the Windows release wrapper so `electron-builder` launches correctly on GitHub Actions.
- Remove the macOS `afterPack` hook dependency on `builder-util`, which was missing in CI and blocked release packaging.
- Keep shipping separate macOS Intel and Apple Silicon artifacts with only the matching `@libsql` native binding bundled into each app.

## Notes

- `v0.1.25` was replaced by this patch release after the original publish workflow failed before artifacts were uploaded.
