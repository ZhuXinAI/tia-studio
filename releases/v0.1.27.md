## Fixed

- Restore the standard Windows and main macOS release workflows to the stable baseline used before the Intel-specific packaging experiments.
- Keep macOS Intel packaging isolated in a separate workflow so it can be tested without risking the original Windows or standard macOS release path.
- Preserve the libsql packaging fixes so released builds still include the native bindings each platform needs at runtime.

## Notes

- The main CI baseline is green again on both macOS and Windows before this tag.
- macOS Intel artifacts now build through a dedicated workflow instead of modifying the default release flow.
