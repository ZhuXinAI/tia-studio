## Fixed

- Fix macOS Intel app packaging so the shipped app can resolve the correct `@libsql` native binding at runtime.
- Publish separate macOS Intel and Apple Silicon artifacts instead of a larger universal app bundle.
- Keep only the active macOS `@libsql` native binary in each packaged app to reduce artifact size.
- Route `electron-builder` through a mirror-safe wrapper so DMG builds do not inherit the wrong Electron mirror URL.

## Notes

- Build scripts now include `pnpm run build:mac:x64` and `pnpm run build:mac:arm64`.
