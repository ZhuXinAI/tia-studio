## Fixed

- Fix macOS universal release packaging so both Intel and Apple Silicon libsql native bindings are bundled correctly.
- Keep electron-builder from trying to re-merge libsql's prebuilt native binaries during universal app assembly.

## Notes

- This patch follows `0.1.22` and is focused on getting the release pipeline green across macOS and Windows.
