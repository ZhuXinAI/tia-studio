export function ProvidersSettingsPage(): React.JSX.Element {
  return (
    <section>
      <h1>Model Provider Settings</h1>
      <p>Configure provider credentials, API host, and selected model.</p>
    </section>
  )
}
